requests

# Pinned together deliberately -- torch 2.2.2 is the newest wheel available
# in this environment's package index, and it needs numpy<2 (its C
# extension's numpy ABI predates numpy 2.0) plus a scipy built against
# numpy<2, plus an older sentence-transformers/transformers that don't
# require torch>=2.5. Installing any of these without pins will pull in
# incompatible newer versions and fail at runtime (not just at import).
numpy<2
scipy<1.13
torch==2.2.2
transformers<4.45
sentence-transformers<3.1

# Script 3 (visualization)
pandas
scikit-learn
plotnine
plotly

# --provider openai
openai

# Optional, only needed for --provider voyage:
# voyageai

# Optional, only needed for --method umap in script 3 (see script comment
# for why it's not a default dependency). On at least one dev machine this
# failed to install: umap-learn -> numba -> llvmlite has no prebuilt wheel
# for that Python/platform, and building from source needs a matching
# system LLVM that wasn't present. `pip install umap-learn` and see if it
# works for you before assuming it's broken everywhere.
# umap-learn
